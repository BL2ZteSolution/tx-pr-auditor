interface:
  display_name: "TX PR Auditor"
  short_description: "Audit Final PO against generated ECC output"
  default_prompt: "Use $tx-pr-auditor to generate TSS/TI entitlement from EPMS.xlsx and validate Final PO.xlsx in one controlled run."

policy:
  allow_implicit_invocation: true
